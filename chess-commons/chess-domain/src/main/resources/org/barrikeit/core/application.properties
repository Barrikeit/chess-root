# docker pull postgres:14.4-alpine
# docker run -d --name postgres_chess -e POSTGRES_USER=**** -e POSTGRES_PASSWORD=**** -e POSTGRES_DB=**** -p ****:**** postgres:14.4-alpine

# docker run -d --name generic -e POSTGRES_PASSWORD=user -e POSTGRES_USER=pass -e POSTGRES_DB=barrikeit -p 2345:2345 postgres:14.4-alpine
# docker ps
# docker exec -it generic psql -U user -d barrikeit

